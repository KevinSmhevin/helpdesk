{
  "name": "helpdesk",
  "private": true,
  "workspaces": ["client", "server", "e2e"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:server": "bun run --filter server dev",
    "dev:client": "bun run --filter client dev",
    "test:e2e": "bun run --filter @helpdesk/e2e test"
  },
  "devDependencies": {
    "@eslint/js": "^9",
    "eslint": "^9",
    "eslint-config-prettier": "^10",
    "prettier": "^3",
    "typescript-eslint": "^8"
  }
}
